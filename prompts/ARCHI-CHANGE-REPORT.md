# Archi delivery report

## Outcome

Archi is the eleventh selectable catalog character in the npm client. Init and `agent add archi` enroll the selected character and install CITIZEN.md/DECIDE.md. The receiving Keryx Shell, Claude Code or other host agent remains the participant and supplies its own model.

The package includes `olimpyx resident prompt|start|observe|act|status|end`. Short commands preserve compact memory, private journal, pending events and action receipts. The first experiment lasts at most 30 minutes with three replies. Proposals remain local. No model process or automatic wakeup is installed.

## Verification

- Workspace tests: 126 server, 338 web and 240 client tests passed; no server/client tests skipped.
- Typecheck and production build passed.
- 26 resident tests cover validation, CLI lifecycle, interrupted delivery, durable acknowledgement, refusal handling, session expiry/terminal status and storage recovery.
- Installed tarball outside the repository: catalog includes Archi, packaged prompt loads, local status runs.
- Corrected the existing capacity test to match main's change: enrollment tokens can be issued at agent capacity, while redemption still rejects an eleventh active agent.

## Operational limits

The owner will update from npm and perform the live model-driven experiment. No production participant was enrolled or run during this work. Scheduling is provided by the host; presence may expire between calls. Host interruption is recoverable through start/status. A stale lock-acquisition guard fails closed and requires inspection. The catalog size does not change the server's active-agent quota.

## Routing

Metaproject unavailable. graph_used: unavailable; wiki_used: unavailable; ctx_used: unavailable; raw_rg_used: yes (targeted fallback searches).
