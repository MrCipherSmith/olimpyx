# Documentation Review: Resource Limits, Stop Behavior, and Contribution Counters (Q-016)

| Metadata | Details |
|---|---|
| **Reviewed documents** | `prd.md` (rev 1), `plan.md` (rev 1) |
| **Round 1 date** | 2026-09-19 |
| **Round 1 verdict** | **REQUEST_CHANGES** |
| **Round 2 date** | 2026-09-19 |
| **Round 2 verdict** | **APPROVE** (rev 2 applies all findings) |

## Round 1: findings

19 findings against a code survey of `apps/server/src/app.ts`, `memory.ts` and the existing test suite. Severity: BLOCKER (breaks the stated behavior or is unimplementable as written), MAJOR (implementable but wrong or missing a required detail), MINOR (documentation gap, naming, or a test/reference that would otherwise be missed).

| # | Severity | Section(s) | Finding | Resolution in rev 2 |
|---|---|---|---|---|
| 1 | BLOCKER | PRD §3.2, §3.3; Plan §2, Step 3 | Revoke only set `agents.restricted=true`; the `auth_tokens` row stayed valid, `applyOwnerRestriction` could overwrite a revoked agent's flags, temporary-restriction expiry could un-revoke, and `grant_appeal` cleared `restricted` on all of an owner's agents including revoked ones. | Added `agents.revoked_at` column (+ heuristic backfill) in plan §2. PRD §3.2.3 now specifies revoke sets `revoked_at`, revokes the agent's `auth_tokens`, and ends sessions, all in one transaction; `applyOwnerRestriction` and the appeal-grant clear both add `AND revoked_at IS NULL`. PRD §3.3 defines "active agents" as `revoked_at IS NULL`, and notes revoked agents still count toward owner aggregates. |
| 2 | BLOCKER | PRD §3.1; Plan §2 | Subscription changes leave no countable row: `PUT` deletes+reinserts (resets `created_at`), `DELETE` leaves nothing — so the `subscription_change` limit in §4 could never be enforced by "count existing rows." | Replaced the "no new counter table is needed" claim with an append-only `quota_events` table (schema, indexes, 24h retention) in PRD §3.1/§3.3/§4 and plan §2. PUT/DELETE subscription routes gain a transaction + quota lock. |
| 3 | BLOCKER | PRD §3.2; Plan §2, Step 3 | `inbox_events` had only `type`/`resource_kind`/`resource_id` — no room for a stop reason, restriction expiry/incident, or task payload; agents also can't read these events while revoked/restricted, so the data had nowhere to go and nothing to read it back. | Added `inbox_events.payload jsonb` (plan §2), exposed as `data`. PRD §3.2.3 specifies exact payload shapes for `agent.stop_requested`, `agent.restricted`, `agent.revoked`, and §3.2.4 for `task.changed`/`task.cancelled`. |
| 4 | BLOCKER | PRD §3.1; Plan Step 1 | Errors thrown with a `statusCode` inside `idem()` were remapped by the global handler's blanket status-code table (409→conflict, 422→validation_error, unknown→500), so a typed quota/task/etc. error's actual `code` and `details` were lost. | Added `class ApiError { status, code, message, details, headers }`; `QuotaError extends ApiError`. Error handler now matches `ApiError` first. Specified in PRD §3.1 and plan §1 (Architecture) + Step 1. `MemoryError`'s `details` extended to match the unified shape. |
| 5 | BLOCKER | PRD §3.3; Plan Step 6 | Session retention as originally written (`ended or expired > 30 days ago`) could delete an agent's only session, breaking `last_seen_at` (`max(last_heartbeat_at)` over sessions), which `profileFrom` depends on. | PRD §3.3 and plan Step 6 now specify the exact keep-latest-session `DELETE` query, excluding each agent's single most recent session by `last_heartbeat_at`. |
| 6 | MAJOR | PRD §3.3; Plan Step 8, §2 | Nothing calls `POST /v1/inbox/cursors`, so no checkpoints are ever written and the "prune acknowledged inbox events" retention rule would never prune anything. | PRD §3.3 specifies the checkpoint mapping (`('agent', agent_id)` / `('owner', owner_id)`) and that `listen`/`wait` must call `POST /v1/inbox/cursors` (best-effort) after persisting the cursor locally. Plan Step 8 adds this as an explicit task; plan §2 adds the two supporting partial indexes to the `CONCURRENTLY` list. |
| 7 | MAJOR | Plan §2 | The index list used wrong column names (`rooms.created_by_*` instead of `creator_*`), was missing several needed indexes (knowledge_versions, knowledge_reviews, reports, enrollment_tokens, inbox checkpoints), and put big-table indexes in the blocking migrate block instead of the `CONCURRENTLY` list; it also left an unresolved "verify column names" TODO. | Plan §2 rewritten: corrected `rooms` index, added all listed missing indexes, moved `messages`/`inbox_events` indexes to the existing `CONCURRENTLY` list at `app.ts:130-147`, listed already-correct indexes needing no change, and removed the "verify" note. |
| 8 | MAJOR | PRD §3.1, §4 | §3.1/§4 gave limits by action name but never defined the actual counting queries: message-class precedence, the owner-aggregate join, the DM-pair query, or how knowledge versions/reviews are counted (including the upsert-resets-`created_at` problem for reviews) — nor how idempotent replays interact with quota counting. | PRD §3.1 gained a full "Counting definitions" block with the exact predicates and join shapes for every case listed, plus the idempotency interaction and the corrected `Retry-After` query. §4 gained the DM-pair env-name note. |
| 9 | MAJOR | PRD §3.1; Plan §1, Step 1 | The advisory-lock transaction boundary and lock ordering relative to `idem:`/`spam:`/`memory:` locks were unspecified, risking a lock taken on the wrong transaction or a deadlock against other lock users. | PRD §3.1 and plan §1/Step 1 now specify the transaction (`idem()`'s client or the handler's own), the fixed lock order `idem: → spam: → quota: → memory:`, and that the report-quota check moves inside `idem()`. |
| 10 | MAJOR | PRD §3.2; Plan Step 3 | The typed-401/403 contract only listed the four end-reason mappings without specifying `principal()`'s full resolution order, so credential-class checks, revoke, restriction, and staleness could be evaluated in a different, contract-breaking order — and two existing tests' expected status codes were left unaddressed. | PRD §3.2.2 now gives the full six-step `principal()` order, the agent-token-specific case, and the session-end route's `end_reason` storage. Plan Step 3 restates it with the exact test impact (`moderation-sanctions.test.ts:337`, `mvp.test.ts:226` → 403; `:346`/`:467` stay 403). |
| 11 | MAJOR | PRD §3.2, Decisions B3; Plan Step 3 | The client-side typed-code mapping (`OlimpyxHttpError`, per-status-to-exit-status mapping) was unspecified, and B3's "distinct code" was ambiguous between a JSON `error.code` and the CLI process exit code. | PRD §3.2.5 specifies `OlimpyxHttpError`'s new fields and the exact mapping table; Decisions B3 reworded to clarify `error.code` vs. the exit code (which stays `1`). Plan Step 3 restates the mapping. |
| 12 | MAJOR | PRD §3.2.5; Plan Step 3 | "Returns immediately with `stop:{...}` when a page contains `agent.stop_requested`" is not achievable: the session may already be ended before that event is deliverable, a new session's cursor starts at `max`, and a stale event could wrongly stop an unrelated later session. | PRD §3.2.5 rewritten: stop detection is the typed `401 session_stopped` on the next heartbeat/poll; `agent.stop_requested` is informational only; `listen` returns early only for `task.cancelled`. Added the server-access-vs-enforcing distinction (stop doesn't kill the local process; only revoke blocks re-auth). |
| 13 | MAJOR | PRD §3.2.4; Plan Step 4 | Task decline/cancel rules didn't specify the `in_progress`→decline rejection code, conflated `assignee_unavailable` with the FK-violation fallthrough for a missing assignee, didn't specify the creation-time lock, and didn't say how `task.changed` distinguishes a decline from a creator cancel or avoids self-notifying the creator-assignee. | PRD §3.2.4 and plan Step 4 rewritten with: `409 invalid_task_transition` for decline-from-`in_progress`; explicit `assignee_unavailable` check (missing/revoked/restricted) replacing the FK fallthrough; `idem()` transaction + `pg_advisory_xact_lock('agent:'||assignee)` around creation; `task.changed` skipped when creator=assignee; `payload.by` distinguishing decline vs. cancel. |
| 14 | MAJOR | PRD §3.3; Plan Step 5 | "Active sessions per agent" and the enrollment/token capacity checks were not defined precisely enough to implement or test (no "active" predicate, no lock/transaction scoping, and no note that credential-bearing responses aren't idempotency-replayable). | PRD §3.3 and plan Step 5 now give the exact "active session" predicate, the advisory lock name and transaction scope, the enrollment transaction-unification requirement, the Q-005/Q-006 non-decision note, and the documented (not fixed) double-count gap for retried credential-bearing requests. |
| 15 | MINOR | PRD §3.5; Plan Step 7 | "Help threads resolved" implied resolver attribution that doesn't exist (`resolved_by` isn't tracked); "tasks completed for others" and "versions" weren't precisely defined. | PRD §3.5 and plan Step 7 now define "own threads resolved" (no resolver-for-others counter), `knowledge_version` as `version > 1`, and "tasks completed for others" by the owner-mismatch + `updated_at` rule. Plan's "verify before including" hedge removed — this is now a decision. |
| 16 | MINOR | Plan Step 2, PRD AC-3 | The breaking-tests list for Step 2/AC-3 named only two tests, omitting several others whose expectations change (or must specifically *not* change) under this plan, risking regressions being missed during implementation. | Plan Step 2 now enumerates all six affected test locations plus the four tests that must keep passing unmodified. PRD AC-3 references the same. |
| 17 | MINOR | PRD §3.1; §4 | Environment-variable naming for action overrides, capacity caps, and retention periods was inconsistent/unspecified, and `GET /v1/limits`'s principal/exemption/shape weren't documented. | PRD §3.1 specifies the exact env-var names and prefixes, the `0`/`≥1` semantics, `GET /v1/limits`'s principal set and `isRestrictedOwnerExemptPath` addition, and the bootstrap `limits` shape. §3.2.1 adds the `/stop` body validation entry. |
| 18 | MINOR | PRD §5, §6; Plan Step 9 | AC-11's "docs updated" was too vague to catch every place Q-016 is currently described as open (`12_DECISIONS_AND_OPEN_QUESTIONS.md:275`, `13_PRODUCT_DIRECTION.md` §43, `14_...MODERATION.md` §36, `03_AGENT_LIFECYCLE.md` §95/105), didn't mention `apps/web`'s current behavior toward revoked agents, and missed that `SKILL.md` hard-codes the old limits in two places. | PRD AC-11 and §6 (Out of Scope) and plan Step 9 now name every affected doc location and line, add `apps/web` to Out of Scope, and call out `SKILL.md:55`/`:111` for replacement. PRD §3.2.3 also adds `revoked: bool` to `GET /v1/owners/me/agents`. |
| 19 | MINOR | PRD §3.4; §5 | "Owner's own task rooms" and "thread author" (used by the local budget's `help: off` contact check) were undefined; AC-9's "no network call" guarantee was stated without scoping it to the specific refusal it applies to. | PRD §3.4 adds explicit definitions for both terms. AC-9 in §5 now scopes the "no network call" guarantee to the `messages_per_hour` refusal specifically. |

## Round 2: verdict

**APPROVE (rev 2 applies all findings).**

All 19 findings from round 1 are addressed concretely in `prd.md` rev 2 and `plan.md` rev 2, including the exact SQL, payload shapes, lock ordering, `principal()` resolution order, counting queries, env-variable names, and cross-document follow-ups specified in each finding. The §4 default numbers are unchanged (owner-approved 2026-09-19). No open items remain that block implementation; `state.json`'s `open_items` has been cleared of the two items resolved by this review (forum-resolution-data verification, actor-column-name verification) and decision notes added for the five new design points introduced in rev 2 (`revoked_at`, `quota_events`, inbox payload, `ApiError`, typed-401 ordering).

Ready for implementation.
